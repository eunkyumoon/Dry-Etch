# Y2O3 Focus Ring 실시간 모니터링 대시보드

## 📋 개요

이 대시보드는 Y2O3 Focus Ring의 마모 상태를 실시간으로 모니터링하고, 잔존 수명(RUL)을 예측하는 웹 기반 대시보드입니다.

## 🎨 주요 기능

### 중앙 네트워크 시각화
- 챔버별 상태를 네트워크 그래프로 시각화
- 실시간 마모 상태에 따른 색상 변화
- 챔버 간 연결 관계 표시

### 좌측 패널
1. **공정 모니터링 및 분석 - RF 파라미터**
   - Vpp, Vdc, Phase 실시간 값 표시
   - RF 파라미터 비교 바 차트

2. **마모 상태 분석**
   - 마모 진행도 게이지 차트 (0-100%)
   - 잔여 두께 및 상태 메트릭

3. **잔존 수명 예측 (RUL)**
   - 예상 RUL 시간
   - 예상 잔여 두께 및 신뢰 구간

### 우측 패널
1. **설비 운영 모니터링 및 분석**
   - 자산 마모도 게이지
   - 누적 시간 및 마모율 메트릭

2. **위험 모니터링 및 조기 경보 - Arcing 위험**
   - Arcing 위험도 게이지
   - 위험 등급 및 위험 요소 분포 도넛 차트

3. **교체 알람**
   - 교체 권장 알람 (긴급/경고/주의)
   - 실시간 알람 레벨 표시

### 하단 패널
- **시간 시리즈 모니터링**
  - Vpp 시간 시리즈 차트
  - Yttrium Peak 시간 시리즈 차트
  - 마모 진행도 시간 시리즈 차트

## 🚀 실행 방법

### 1. 패키지 설치

```bash
pip install -r requirements.txt
```

### 2. 대시보드 실행

```bash
streamlit run dashboard.py
```

### 3. 브라우저 접속

대시보드가 자동으로 브라우저에서 열립니다. 기본 주소는 `http://localhost:8501`입니다.

## ⚙️ 사용 방법

### 사이드바 설정
- **챔버 선택**: 모니터링할 챔버 선택 (Chamber1-4)
- **누적 시간**: 시뮬레이션할 누적 시간 설정 (0-2000시간)
- **자동 새로고침**: 실시간 업데이트 활성화/비활성화
- **새로고침 간격**: 자동 새로고침 간격 설정 (1-60초)

### 대시보드 구성 요소

#### 메트릭 카드
- 현재 값을 큰 숫자로 표시
- 변화량을 델타 값으로 표시

#### 게이지 차트
- 0-100% 범위의 게이지 차트
- 색상 구간:
  - 파란색: 정상 (0-50%)
  - 주황색: 주의 (50-80%)
  - 빨간색: 위험 (80-100%)

#### 도넛 차트
- 위험 요소 분포를 시각화
- 각 위험 요소의 비율 표시

#### 시간 시리즈 차트
- 과거 데이터 추이 확인
- 호버 시 상세 정보 표시

## 🎨 디자인 특징

- **어두운 테마**: 어두운 파란색 배경 (#0a0e27, #1a1f3a)
- **네온 효과**: 발광하는 파란색 (#00d4ff) 강조
- **반응형 레이아웃**: 다양한 화면 크기 지원
- **인터랙티브 차트**: Plotly 기반 인터랙티브 시각화

## 📊 데이터 소스

대시보드는 `refactored/` 모듈의 다음 컴포넌트를 사용합니다:

- `DataCollector`: RF 및 OES 데이터 수집
- `WearEstimator`: 마모 상태 추정
- `RULPredictor`: 잔존 수명 예측
- `AnomalyDetector`: 이상 탐지

## 🔧 커스터마이징

### 색상 변경
`dashboard.py`의 `CUSTOM_CSS` 섹션에서 색상을 변경할 수 있습니다:

```python
# 주요 색상
- 배경: #0a0e27, #1a1f3a
- 강조색: #00d4ff
- 경고: #ffa500
- 위험: #ff0000
```

### 차트 스타일 변경
각 차트 생성 함수 (`create_gauge_chart`, `create_donut_chart` 등)에서 스타일을 수정할 수 있습니다.

## 📝 참고 사항

- 대시보드는 시뮬레이션 데이터를 사용합니다
- 실제 데이터를 연결하려면 `DataCollector`의 데이터 소스를 수정하세요
- 자동 새로고침을 활성화하면 설정된 간격마다 자동으로 업데이트됩니다

## 🐛 문제 해결

### 대시보드가 열리지 않는 경우
1. 포트 8501이 사용 중인지 확인
2. 다른 포트 사용: `streamlit run dashboard.py --server.port 8502`

### 차트가 표시되지 않는 경우
1. Plotly 설치 확인: `pip install plotly`
2. 브라우저 콘솔에서 오류 확인

### 모듈을 찾을 수 없는 경우
1. `refactored/` 폴더가 같은 디렉토리에 있는지 확인
2. Python 경로 확인

## 📚 추가 리소스

- [Streamlit 문서](https://docs.streamlit.io/)
- [Plotly 문서](https://plotly.com/python/)
- 프로젝트 README: `README.md`

